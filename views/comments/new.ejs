<!--including html boilerplate (header)-->
<%- include("../partials/header") %> 


<div class="container">
    <div class="row"></div>
        <h1 style="text-align: center;">Add a new comment..<%= campground.name %> </h1>
        <div style="width: 30%; margin: 35px auto;">
            <form action="/campgrounds/<%= campground._id %>/comments", method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="comment[text]" placeholder="Comment">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-default btn-block btn-success">Submit</button>
                </div>
            </form>
            <a href="/">Go back</a>
        </div>

     
    </div>
</div>

<!--including html boilerplate (footer)-->
<%- include("../partials/footer") %>